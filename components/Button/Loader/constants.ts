import { LoaderSizeTypes, LoaderSkinTypes } from '../types';

/**
 * Spinner Style Display Options
 */
export const SPINNER_SKIN: LoaderSkinTypes = {
  standard: 'grey',
  inline: 'standard',
  grey: 'standard',
  inlineGrey: 'standard',
  light: 'green'
}

/**
 * Spinner Size Display Options
 */
export const SPINNER_SIZE: LoaderSizeTypes = {
  large: 'small',
  medium: 'small',
  small: 'tiny',
  tiny: 'tiny'
}

/**
 * Spinner Background Display Options
 */
export const SPINNER_BG: LoaderSkinTypes = {
  standard: 'bg-A00',
  inline: 'bg-white',
  grey: 'bg-C00',
  inlineGrey: 'bg-white',
  light: 'bg-white'
}