export const argTypes = {
  position: {
    control: 'select',
    options: [
      "start",
      "center"
    ],
    table: {
      defaultValue: { summary: 'start' }
    }
  }
}

export const args = {};